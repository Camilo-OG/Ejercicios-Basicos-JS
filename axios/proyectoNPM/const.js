// ECMAScript  ---> import y export

export const number = 50;

export const obtenerNombreCompleto = (nombre, apellido) => {
  return `El nombre completo de la persona es ${nombre} ${apellido}`
}
export const imprimirEdad = edad => {
  return `La edad es: ${edad}`
}